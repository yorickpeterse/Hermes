#!/usr/bin/env bash
set -e

echo 'Updating codebase...'

bundle install --deployment

bundle exec rake db:migrate --trace

echo 'Restarting daemon...'

bundle exec ruby ./bin/hermes -k
bundle exec ruby ./bin/hermes -d
